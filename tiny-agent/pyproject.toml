[project]
name = "tiny-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.26.0",
    "python-dotenv>=1.1.0",
    "pyyaml>=6.0",
]
